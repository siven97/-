# 备用方案：使用 Netlify（2分钟部署）

如果GitHub Pages设置比较麻烦，可以使用Netlify，更简单快速！

## 🚀 方法一：Netlify Drop（最快，无需注册）

### 步骤：

1. **访问Netlify Drop**
   ```
   https://app.netlify.com/drop
   ```

2. **拖拽文件**
   - 直接拖拽 `wifi-info.html` 文件到页面
   - 等待上传（几秒钟）

3. **获取URL**
   - 自动生成类似：`https://xxx-xxx-xxx.netlify.app/wifi-info.html`
   - 复制这个URL

4. **重新生成二维码**
   ```bash
   # 编辑 generate_qrcode.py
   # 将 url 改为 Netlify 的URL
   # 然后运行：
   python3 generate_qrcode.py
   ```

5. **完成！**
   - 立即可以扫码使用
   - 无需等待

## 🌐 方法二：使用 GitHub Gist（永久有效）

### 步骤：

1. **访问GitHub Gist**
   ```
   https://gist.github.com
   ```

2. **创建新Gist**
   - 点击右上角 "+" → "New gist"
   - Filename: `wifi-info.html`
   - 粘贴 `wifi-info.html` 的全部内容
   - 选择 "Create public gist"

3. **获取Raw链接**
   - 点击右上角的 "Raw" 按钮
   - 复制浏览器地址栏的URL
   - 类似：`https://gist.githubusercontent.com/siven97/xxx/raw/xxx/wifi-info.html`

4. **重新生成二维码**
   - 使用这个Raw URL
   - 运行 `python3 generate_qrcode.py`

## 📊 方案对比

| 方案 | 速度 | 难度 | 稳定性 | 推荐度 |
|------|------|------|--------|--------|
| Netlify Drop | ⭐⭐⭐⭐⭐ | ⭐☆☆☆☆ | ⭐⭐⭐⭐☆ | ⭐⭐⭐⭐⭐ |
| GitHub Gist | ⭐⭐⭐⭐☆ | ⭐⭐☆☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐☆ |
| GitHub Pages | ⭐⭐⭐☆☆ | ⭐⭐⭐☆☆ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐☆☆ |

## 🎯 推荐方案

**如果想要最快**：使用 **Netlify Drop**
- 2分钟完成
- 拖拽即可
- 立即生效

**如果想要最稳定**：使用 **GitHub Gist**
- 永久有效
- 简单可靠
- 5分钟完成

## 💡 Netlify Drop 详细步骤

1. 打开浏览器，访问：https://app.netlify.com/drop

2. 看到页面中间有个大框，写着 "Drag & drop your site folder here"

3. 从文件夹中拖拽 `wifi-info.html` 到这个框里

4. 等待几秒钟，会显示：
   ```
   Your site is live!
   https://xxx-xxx-xxx.netlify.app
   ```

5. 完整URL是：
   ```
   https://xxx-xxx-xxx.netlify.app/wifi-info.html
   ```

6. 复制这个URL，修改 `generate_qrcode.py`：
   ```python
   url = "https://xxx-xxx-xxx.netlify.app/wifi-info.html"
   ```

7. 运行：
   ```bash
   python3 generate_qrcode.py
   ```

8. 完成！扫码测试

## ✅ 优势

- **无需注册**：Netlify Drop不需要账号
- **即时生效**：上传后立即可用
- **免费使用**：完全免费
- **稳定可靠**：Netlify是专业的托管服务

## 🔄 如果需要更新

Netlify Drop的缺点是每次更新需要重新上传，会生成新的URL。

如果需要经常更新，建议：
1. 注册Netlify账号
2. 或使用GitHub Pages（一次设置，永久使用）

---

**推荐**: 先用Netlify Drop快速测试，确认功能正常后，再考虑使用GitHub Pages作为长期方案。
